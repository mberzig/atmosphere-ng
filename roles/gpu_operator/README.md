# `gpu_operator`

This role installs and configures the NVIDIA GPU Operator, which manages
the driver, the container toolkit, the device plugin, the MIG manager and
the DCGM exporter of the GPU nodes, making the GPUs consumable by the
containerized workloads either as full devices or as MIG instances.

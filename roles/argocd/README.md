# `argocd`

This role installs and configures Argo CD, which is the deployment channel
of the application catalog: applications are installed by committing their
definition to a Git repository which Argo CD reconciles, instead of being
deployed directly.
